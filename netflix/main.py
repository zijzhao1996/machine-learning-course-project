import numpy as np
import kmeans
import common
import naive_em
import em

X = np.loadtxt("toy_data.txt")

# TODO: Your code here

#  Your task is to run the K-means algorithm on this data using the implementation we have provided in kmeans.py 
# Initialize K-means using common.init(X, K, seed, 
# where  𝐾  is the number of clusters and seed is the random see used to randomly initialize the parameters.
